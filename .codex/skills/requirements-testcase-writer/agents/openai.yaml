interface:
  display_name: "需求转测试用例"
  short_description: "根据需求文档生成、补充并更新仓库内XMind测试用例"
  default_prompt: "Use $requirements-testcase-writer to generate or update XMind-style test cases from files under `requirments/` and write the result into `testcases/`."

policy:
  allow_implicit_invocation: true
